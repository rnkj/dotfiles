# Initialization
- Identify the project's short name and determine the H1 title for `AGENTS.md`.
- Review the project's file structure and summarize it in the `## File structure` section of `AGENTS.md` at approximately the same level of detail as `tree -L 1`.
- After completing the above two tasks, remove the entry for this file from `.agents/memory/MEMORY.md`.
